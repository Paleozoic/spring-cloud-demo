server:
  port: 20003

spring:
  profiles:
    active: dev
  application:
    name: ms-gateway
  cloud:
    config:
      uri: http://localhost:20000
      label: config-repo


# 对外提供的网关路由，或者用于界面应用对服务端的请求路由。可用nginx替代
zuul:
  routes:
    ms_hello:
      path: /hello/**
    ms_world:
      path: /world/**
